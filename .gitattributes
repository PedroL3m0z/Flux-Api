# Normalize line endings to LF in the repo (cross-platform safety).
* text=auto eol=lf

# Shell scripts must be LF or they break in Linux containers (exit 127).
*.sh text eol=lf

# Binary assets — never normalize.
*.png binary
*.jpg binary
*.ico binary
